version: 2
updates:
- package-ecosystem: nuget
  directory: "/"
  schedule:
    interval: daily
  open-pull-requests-limit: 10
  ignore:
    - dependency-name: "Humanizer"
    # Avoid PRI errors "Processing Resources failed with error: Duplicate Entry."
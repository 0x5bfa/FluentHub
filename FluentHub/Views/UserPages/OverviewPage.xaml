<Page
    x:Class="FluentHub.Views.UserPages.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:item="using:FluentHub.Models.Items"
    xmlns:local="using:FluentHub.Views.UserPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:uc="using:FluentHub.UserControls.Block"
    xmlns:vm="using:FluentHub.ViewModels.UserPages"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:OverviewViewModel x:Name="ViewModel" />
    </Page.DataContext>
    <Page.Resources>
        <Style
            x:Key="PinnedItemBlockButtonStyle"
            BasedOn="{StaticResource DefaultButtonStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource ExpanderHeaderBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ExpanderHeaderBorderBrush}" />
            <Setter Property="BorderThickness" Value="{ThemeResource ExpanderHeaderBorderThickness}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="{StaticResource ExpanderContentPadding}" />
            <Setter Property="MinHeight" Value="{StaticResource ExpanderMinHeight}" />
            <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid
                x:Name="UserSpecialReadmeBlock"
                Height="400"
                Margin="0,0,0,6"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                RowSpacing="6"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon
                            FontFamily="{StaticResource Octions}"
                            FontSize="16"
                            Glyph="&#xEA38;" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="14"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            Text="README" />
                    </StackPanel>
                    <HyperlinkButton
                        x:Name="ReadMeLink"
                        Padding="4"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        FontFamily="Cascadia Code"
                        FontSize="12"
                        Foreground="{ThemeResource ApplicationForegroundThemeBrush}" />
                </Grid>
                <Grid
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <WebView
                        x:Name="UserSpecialReadmeWebView"
                        CanBeScrollAnchor="True"
                        DefaultBackgroundColor="Transparent"
                        Loaded="UserSpecialReadmeWebView_Loaded"
                        NavigationStarting="UserSpecialReadmeWebView_NavigationStarting" />
                </Grid>
            </Grid>
            <Grid Grid.Row="1" RowSpacing="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                        FontFamily="{StaticResource Octions}"
                        FontSize="16"
                        Glyph="&#xEA3E;" />
                    <TextBlock
                        FontSize="14"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="Pinned" />
                </StackPanel>
                <ScrollViewer
                    Grid.Row="1"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollMode="Disabled">
                    <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Items}" Loaded="ItemsRepeater_Loaded">
                        <muxc:ItemsRepeater.Layout>
                            <muxc:StackLayout Orientation="Horizontal" Spacing="8" />
                        </muxc:ItemsRepeater.Layout>
                        <DataTemplate x:DataType="item:RepoListItem">
                            <uc:RepoBlock
                                x:Name="RepositoryBlock"
                                Width="250"
                                VerticalAlignment="Stretch"
                                DisplayStarButton="False"
                                RepositoryId="{x:Bind RepoId}" />
                        </DataTemplate>
                    </muxc:ItemsRepeater>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Page>

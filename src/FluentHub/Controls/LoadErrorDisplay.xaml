<!--  Copyright (c) 0x5BFA. All rights reserved. Licensed under the MIT License. See the LICENSE.  -->
<UserControl
	x:Class="FluentHub.Controls.LoadErrorDisplay"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:FluentHub.Controls"
	x:Name="Root">

	<Grid Padding="32">
		<StackPanel
			MaxWidth="560"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Spacing="8">
			<PathIcon
				Width="32"
				Height="32"
				HorizontalAlignment="Center"
				Data="{controls:OcticonData Name=Alert24}"
				Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
			<TextBlock
				HorizontalAlignment="Center"
				FontSize="36"
				Style="{StaticResource BodyStrongTextBlockStyle}"
				Text="Something went wrong"
				TextAlignment="Center"
				TextWrapping="Wrap" />
			<TextBlock
				HorizontalAlignment="Center"
				Foreground="{ThemeResource TextFillColorSecondaryBrush}"
				Style="{StaticResource BodyTextBlockStyle}"
				Text="{x:Bind ErrorMessage, Mode=OneWay}"
				TextAlignment="Center"
				TextWrapping="Wrap" />
			<Button
				HorizontalAlignment="Center"
				AutomationProperties.Name="Retry loading"
				Command="{x:Bind RetryCommand, Mode=OneWay}"
				Content="Try again" />
		</StackPanel>
	</Grid>
</UserControl>

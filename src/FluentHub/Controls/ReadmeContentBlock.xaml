<UserControl
	x:Class="FluentHub.Controls.ReadmeContentBlock"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="using:FluentHub.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:FluentHub.Controls"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="300"
	d:DesignWidth="400"
	mc:Ignorable="d">

	<UserControl.Resources>
		<ResourceDictionary>
			<converters:ObjectToBoolConverter x:Key="ObjectToBoolConverter" />
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid
		x:Name="RootReadmeContentBlock"
		Background="{ThemeResource LayerFillColorDefaultBrush}"
		BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
		BorderThickness="1"
		CornerRadius="{StaticResource OverlayCornerRadius}"
		Visibility="{x:Bind Markdown, Converter={StaticResource ObjectToBoolConverter}, Mode=OneWay}">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Grid Padding="16,8,16,4">

			<TextBlock
				VerticalAlignment="Center"
				Style="{StaticResource BodyStrongTextBlockStyle}"
				Text="README" />

			<Button
				Height="32"
				HorizontalAlignment="Right"
				Background="Transparent"
				BorderThickness="0"
				Click="OnEditButtonClick"
				Visibility="{x:Bind EditUri, Converter={StaticResource ObjectToBoolConverter}, Mode=OneWay}">
				<StackPanel Orientation="Horizontal" Spacing="6">
					<FontIcon
						FontSize="14"
						Foreground="{ThemeResource TextFillColorSecondary}"
						Glyph="&#xE70F;" />
					<TextBlock Foreground="{ThemeResource TextFillColorSecondary}" Text="Edit" />
				</StackPanel>
			</Button>

		</Grid>

		<MenuFlyoutSeparator Grid.Row="1" />

		<local:GitHubMarkdownTextBlock
			Grid.Row="2"
			Margin="16,8,16,16"
			BaseUrl="{x:Bind BaseUrl, Mode=OneWay}"
			Text="{x:Bind Markdown, Mode=OneWay}" />

	</Grid>
</UserControl>

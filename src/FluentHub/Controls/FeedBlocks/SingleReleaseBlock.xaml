<UserControl
	x:Class="FluentHub.Controls.FeedBlocks.SingleReleaseBlock"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:FluentHub.Controls.FeedBlocks"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:primer="using:FluentHub.Controls.PrimerControls"
	xmlns:uc="using:FluentHub.Controls"
	d:DesignHeight="300"
	d:DesignWidth="400"
	mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<!--<Grid Padding="16,8">
			<uc:UserAvatar Size="32"
				HorizontalAlignment="Left"
				Label="{x:Bind ViewModel.Details.Sender.Login}"
				AvatarUrl="{x:Bind ViewModel.Details.Sender.AvatarUrl}" />
		</Grid>
		<Rectangle
			Grid.Row="1"
			Height="1"
			Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />-->
		<uc:GitHubMarkdownTextBlock
			Grid.Row="2"
			Margin="16,16"
			MaxHeight="160"
			Text="{x:Bind ViewModel.Details.Release.Description, Mode=OneWay}" />
	</Grid>
</UserControl>

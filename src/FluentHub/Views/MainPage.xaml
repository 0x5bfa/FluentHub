<!--  Copyright (c) 0x5BFA. All rights reserved. Licensed under the MIT License. See the LICENSE.  -->
<UserControl
	x:Class="FluentHub.Views.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
	xmlns:converters="using:FluentHub.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:FluentHub.Helpers"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:models="using:FluentHub.Models"
	xmlns:modelsV4="using:FluentHub.Core.Application.Models"
	xmlns:primer="using:FluentHub.Controls.PrimerControls"
	xmlns:tabsuc="using:FluentHub.Controls.Tabs"
	xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
	xmlns:uc="using:FluentHub.Controls"
	KeyTipPlacementMode="Hidden"
	KeyboardAcceleratorPlacementMode="Hidden"
	mc:Ignorable="d">

	<UserControl.KeyboardAccelerators>
		<KeyboardAccelerator
			Key="T"
			Invoked="OnAddNewTabAcceleratorInvoked"
			Modifiers="Control" />
		<KeyboardAccelerator
			Key="W"
			Invoked="OnCloseTabAcceleratorInvoked"
			Modifiers="Control" />
		<KeyboardAccelerator
			Key="Tab"
			Invoked="OnNextTabAcceleratorInvoked"
			Modifiers="Control" />
		<KeyboardAccelerator
			Key="Tab"
			Invoked="OnPreviousTabAcceleratorInvoked"
			Modifiers="Control,Shift" />
		<KeyboardAccelerator
			Key="Left"
			Invoked="OnGoBackAcceleratorInvoked"
			Modifiers="Menu" />
		<KeyboardAccelerator
			Key="Right"
			Invoked="OnGoForwardAcceleratorInvoked"
			Modifiers="Menu" />
	</UserControl.KeyboardAccelerators>

	<UserControl.Resources>
		<ResourceDictionary>

			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary>
					<SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
					<SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
					<Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
					<CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
				</ResourceDictionary>
			</ResourceDictionary.MergedDictionaries>

			<converters:ObjectToBoolConverter x:Key="ObjectToBoolConverter" />

			<Style x:Key="MeControlButtonStyle" TargetType="Button">
				<Setter Property="VerticalAlignment" Value="Center" />
				<Setter Property="HorizontalAlignment" Value="Center" />
				<Setter Property="UseSystemFocusVisuals" Value="True" />
				<Setter Property="CornerRadius" Value="16" />
				<Setter Property="FocusVisualMargin" Value="-3,-3,-3,-3" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="Button">
							<ContentPresenter
								x:Name="ContentPresenter"
								HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
								VerticalAlignment="{TemplateBinding VerticalAlignment}"
								AutomationProperties.AccessibilityView="Raw"
								Content="{TemplateBinding Content}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								ContentTransitions="{TemplateBinding ContentTransitions}"
								RenderTransformOrigin="0.5,0.5">
								<ContentPresenter.RenderTransform>
									<CompositeTransform />
								</ContentPresenter.RenderTransform>
								<VisualStateManager.VisualStateGroups>
									<VisualStateGroup x:Name="CommonStates">
										<VisualStateGroup.Transitions>
											<VisualTransition GeneratedDuration="0:0:0.2">
												<VisualTransition.GeneratedEasingFunction>
													<CubicEase EasingMode="EaseOut" />
												</VisualTransition.GeneratedEasingFunction>
											</VisualTransition>
										</VisualStateGroup.Transitions>
										<VisualState x:Name="Normal" />
										<VisualState x:Name="PointerOver">
											<VisualState.Setters>
												<Setter Target="ContentPresenter.(UIElement.RenderTransform).(CompositeTransform.ScaleX)" Value="0.94" />
												<Setter Target="ContentPresenter.(UIElement.RenderTransform).(CompositeTransform.ScaleY)" Value="0.94" />
											</VisualState.Setters>
										</VisualState>
										<VisualState x:Name="Pressed">
											<VisualState.Setters>
												<Setter Target="ContentPresenter.(UIElement.RenderTransform).(CompositeTransform.ScaleX)" Value="0.84" />
												<Setter Target="ContentPresenter.(UIElement.RenderTransform).(CompositeTransform.ScaleY)" Value="0.84" />
											</VisualState.Setters>
										</VisualState>
										<VisualState x:Name="Disabled" />
									</VisualStateGroup>
								</VisualStateManager.VisualStateGroups>
							</ContentPresenter>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>

		</ResourceDictionary>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!--  TabView & Window Draggable Area  -->
		<Grid x:Name="TitleBarContentGrid">

			<ProgressBar
				x:Name="PageLoadingProgreeBar"
				VerticalAlignment="Top"
				x:Load="{x:Bind ViewModel.TaskIsInProgress, Mode=OneWay}"
				IsIndeterminate="True" />

			<tabsuc:CustomTabView
				x:Name="CustomCustomTabView"
				Title="{x:Bind CustomCustomTabView.SelectedItem.Chrome.Header, Mode=OneWay}"
				Grid.Column="3"
				Height="40"
				Margin="0,0,0,0"
				VerticalAlignment="Bottom"
				Background="Transparent"
				Loaded="OnCustomCustomTabViewLoaded"
				SelectionChanged="OnTabViewSelectionChanged" />

		</Grid>

		<!--  Content Area  -->
		<Grid Grid.Row="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Grid
				Grid.Row="0"
				Padding="16,12,16,12"
				Background="{ThemeResource AppThemeGlobalNavigationBarBackgroundBrush}"
				RowSpacing="12">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<Grid Margin="8,0" ColumnSpacing="8">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
						</Grid.ColumnDefinitions>

						<StackPanel Orientation="Horizontal" Spacing="8">

							<!--  Root Navigation Pane  -->
							<!--<Button
								x:Name="LeftSideNavigationViewOpenerButton"
								Width="32"
								Height="32"
								Padding="0"
								Click="LeftSideNavigationViewOpenerButton_Click"
								IsEnabled="False">
								<Viewbox Width="16">
									<AnimatedIcon>
										<AnimatedIcon.Source>
											<animatedvisuals:AnimatedGlobalNavigationButtonVisualSource />
										</AnimatedIcon.Source>
										<AnimatedIcon.FallbackIconSource>
											<SymbolIconSource Symbol="GlobalNavigationButton" />
										</AnimatedIcon.FallbackIconSource>
									</AnimatedIcon>
								</Viewbox>
							</Button>-->

							<!--  Backward  -->
							<Button
								x:Name="ScreenBackwardNavigationButton"
								Width="32"
								Height="32"
								Padding="0"
								Command="{x:Bind ViewModel.GoBackCommand, Mode=OneWay}"
								ToolTipService.ToolTip="{helpers:ResourceString Name=GoBackButton/ToolTipService/Tooltip}">
								<Viewbox Width="16">
									<AnimatedIcon>
										<AnimatedIcon.Source>
											<animatedvisuals:AnimatedBackVisualSource />
										</AnimatedIcon.Source>
										<AnimatedIcon.FallbackIconSource>
											<SymbolIconSource Symbol="Back" />
										</AnimatedIcon.FallbackIconSource>
									</AnimatedIcon>
								</Viewbox>
							</Button>

							<!--  Forward  -->
							<Button
								x:Name="ScreenForwardNavigationButton"
								Width="32"
								Height="32"
								Padding="0"
								Command="{x:Bind ViewModel.GoForwardCommand, Mode=OneWay}"
								ToolTipService.ToolTip="{helpers:ResourceString Name=GoForwardButton/ToolTipService/Tooltip}">
								<Viewbox Width="16">
									<AnimatedIcon>
										<AnimatedIcon.Source>
											<animatedvisuals:AnimatedBackVisualSource />
										</AnimatedIcon.Source>
										<AnimatedIcon.FallbackIconSource>
											<SymbolIconSource Symbol="Back" />
										</AnimatedIcon.FallbackIconSource>
										<AnimatedIcon.RenderTransform>
											<RotateTransform Angle="180" />
										</AnimatedIcon.RenderTransform>
									</AnimatedIcon>
								</Viewbox>
							</Button>

							<!--  Refresh  -->
							<Button
								x:Name="ScreenReloadNavigationButton"
								Width="32"
								Height="32"
								Padding="0"
								Command="{x:Bind ViewModel.ReloadCommand, Mode=OneWay}"
								ToolTipService.ToolTip="Refresh">
								<FontIcon FontSize="14" Glyph="&#xE72C;" />
							</Button>
						</StackPanel>

						<StackPanel Grid.Column="1" Orientation="Horizontal">
							<Button
								Height="32"
								Background="Transparent"
								BorderThickness="0"
								Visibility="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.Context.SecondaryText, Converter={StaticResource ObjectToBoolConverter}, ConverterParameter=Invert, Mode=OneWay}">
								<TextBlock
									VerticalAlignment="Center"
									Style="{StaticResource BodyStrongTextBlockStyle}"
									Text="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.Context.PrimaryText, Mode=OneWay}" />
							</Button>
							<StackPanel Orientation="Horizontal" Visibility="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.Context.SecondaryText, Converter={StaticResource ObjectToBoolConverter}, Mode=OneWay}">
								<Button Background="Transparent" BorderThickness="0">
									<TextBlock
										VerticalAlignment="Center"
										Style="{StaticResource BodyTextBlockStyle}"
										Text="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.Context.PrimaryText, Mode=OneWay}" />
								</Button>
								<TextBlock
									VerticalAlignment="Center"
									Foreground="{ThemeResource TextFillColorSecondaryBrush}"
									Style="{StaticResource BodyTextBlockStyle}"
									Text="/" />
								<Button Background="Transparent" BorderThickness="0">
									<TextBlock
										VerticalAlignment="Center"
										Style="{StaticResource BodyStrongTextBlockStyle}"
										Text="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.Context.SecondaryText, Mode=OneWay}" />
								</Button>
							</StackPanel>
						</StackPanel>

						<StackPanel
							x:Name="NavigationBarTopRightSideContentStackPanel"
							Grid.Column="2"
							Orientation="Horizontal"
							Spacing="8">

							<!--  Search GitHub  -->
							<!--<TextBox
								Width="256"
								Height="32"
								IsEnabled="False"
								PlaceholderText="Search GitHub" />-->

							<!--  Add New  -->
							<!--<DropDownButton
								x:Name="AddNewItemButton"
								Width="64"
								Height="32"
								IsEnabled="False">
								<FontIcon FontSize="16" Glyph="&#xE710;" />
							</DropDownButton>-->

							<!--  User Icon  -->
							<Button
								x:Name="UserNotificationInBoxButton"
								Width="32"
								Height="32"
								Padding="0"
								Click="UserNotificationInBoxButton_Click">
								<FontIcon FontSize="16" Glyph="&#xEA8F;" />
							</Button>

							<!--  Account  -->
							<Button
								x:Name="ProfileButton"
								Width="36"
								Height="36"
								AutomationProperties.Name="Account menu"
								Style="{StaticResource MeControlButtonStyle}"
								ToolTipService.ToolTip="Account">
								<Border
									Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
									BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
									BorderThickness="1"
									CornerRadius="14">
									<uc:UserAvatar
										AvatarUrl="{x:Bind ViewModel.SignedInUser.AvatarUrl, Mode=OneWay}"
										DisplayName="{x:Bind ViewModel.SignedInUser.Login, Mode=OneWay}"
										Size="28" />
								</Border>
								<Button.Flyout>
									<MenuBarItemFlyout
										x:Name="LeftSideViewerIconMenuFlyout"
										Opening="LeftSideViewerIconMenuFlyout_Opening"
										Placement="BottomEdgeAlignedRight">
										<MenuBarItemFlyout.MenuFlyoutPresenterStyle>
											<Style TargetType="MenuFlyoutPresenter">
												<Setter Property="Width" Value="96" />
											</Style>
										</MenuBarItemFlyout.MenuFlyoutPresenterStyle>
										<MenuFlyoutItem IsEnabled="false" Text="Set status" />
										<MenuFlyoutSeparator />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="YourProfile"
											Text="Your profile" />
										<MenuFlyoutSeparator />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="YourRepositories"
											Text="Your repositories" />
										<MenuFlyoutItem
											IsEnabled="false"
											Tag="YourProjects"
											Text="Your projects" />
										<MenuFlyoutItem
											IsEnabled="false"
											Tag="YourCodespaces"
											Text="Your codespaces" />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="YourOrganizations"
											Text="Your organizations" />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="YourStars"
											Text="Your stars" />
										<MenuFlyoutItem IsEnabled="false" Text="Your sponsors" />
										<MenuFlyoutSeparator />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="AppSettings"
											Text="App settings" />
										<MenuFlyoutSeparator />
										<MenuFlyoutItem
											Click="LeftSideViewerIconMenuFlyoutItem_Click"
											Tag="AppSignOut"
											Text="Sign out" />
									</MenuBarItemFlyout>
								</Button.Flyout>
							</Button>
						</StackPanel>

					</Grid>

					<uc:SelectorBarEx
						Grid.Row="1"
						Margin="0,0,0,-16"
						ItemsSource="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.NavigationBarItems, Mode=OneWay}"
						SelectedValue="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.SelectedNavigationBarItem, Mode=TwoWay}"
						Visibility="{x:Bind CustomCustomTabView.SelectedItem.NavigationBar.IsNavigationBarShown, Converter={StaticResource ObjectToBoolConverter}, Mode=OneWay}" />
				</Grid>

			</Grid>

			<Rectangle
				Grid.Row="1"
				Height="1"
				Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

			<Grid
				Grid.Row="2"
				Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}"
				Opacity="0.5" />

			<ContentPresenter
				x:Name="RootScreenPresenter"
				Grid.Row="2"
				Content="{x:Bind CustomCustomTabView.SelectedItem.CurrentScreen.View, Mode=OneWay}" />

			<InfoBar
				Grid.Row="2"
				Margin="16"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Top"
				IsOpen="{x:Bind CustomCustomTabView.SelectedItem.Chrome.HasError, Mode=OneWay}"
				Message="{x:Bind CustomCustomTabView.SelectedItem.Chrome.ErrorMessage, Mode=OneWay}"
				Severity="Error"
				Title="Navigation failed"
				Canvas.ZIndex="1" />

			<Grid Grid.RowSpan="3">
				<NavigationView
					x:Name="LeftSideNavigationView"
					IsBackButtonVisible="Collapsed"
					IsPaneOpen="True"
					IsPaneToggleButtonVisible="False"
					PaneClosed="LeftSideNavigationView_PaneClosed"
					PaneDisplayMode="LeftMinimal"
					Visibility="Collapsed" />
			</Grid>

		</Grid>

		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState x:Name="SmallAdaptiveTriggerVisualState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="MainNavView.PaneDisplayMode" Value="LeftCompact" />
						<Setter Target="MainNavView.IsPaneToggleButtonVisible" Value="True" />
					</VisualState.Setters>
				</VisualState>
				<VisualState x:Name="LargeAdaptiveTriggerVisualState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="1024" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="MainNavView.PaneDisplayMode" Value="Left" />
						<Setter Target="MainNavView.IsPaneToggleButtonVisible" Value="False" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
	</Grid>
</UserControl>

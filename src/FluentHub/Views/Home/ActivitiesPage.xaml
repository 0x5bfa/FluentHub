<Page
    x:Class="FluentHub.Views.Home.ActivitiesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:block="using:FluentHub.UserControls.Blocks"
    xmlns:bvm="using:FluentHub.ViewModels.UserControls.Blocks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Margin="18,0,18,4"
            Orientation="Horizontal"
            Spacing="8">
            <FontIcon FontSize="16" Glyph="&#xECAD;" />
            <TextBlock
                VerticalAlignment="Center"
                FontSize="14"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Activities" />
        </StackPanel>

        <muxc:InfoBar
            Title="Something went wrong"
            Grid.Row="1"
            Margin="18,0"
            IsClosable="False"
            IsOpen="{x:Bind ViewModel.RefreshActivitiesCommand.ExecutionTask.IsFaulted, Mode=OneWay}"
            Message="{x:Bind ViewModel.RefreshActivitiesCommand.ExecutionTask.Exception.InnerException.Message, Mode=OneWay}"
            Severity="Error">
            <muxc:InfoBar.ActionButton>
                <HyperlinkButton
                    Command="{x:Bind ViewModel.RefreshActivitiesCommand}"
                    CommandParameter="{Binding}"
                    Content="Refresh" />
            </muxc:InfoBar.ActionButton>
        </muxc:InfoBar>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <muxc:ItemsRepeater Margin="18,4,18,18" ItemsSource="{x:Bind ViewModel.Activities, Mode=OneWay}">
                <DataTemplate x:DataType="bvm:ActivityBlockViewModel">
                    <block:ActivityBlock ViewModel="{x:Bind}" />
                </DataTemplate>
            </muxc:ItemsRepeater>
        </ScrollViewer>

        <muxc:ProgressRing Grid.RowSpan="3" IsActive="{x:Bind ViewModel.RefreshActivitiesCommand.IsRunning, Mode=OneWay}" />
    </Grid>
</Page>
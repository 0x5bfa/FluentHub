<UserControl
	x:Class="FluentHub.Shared.Controls.Views.GitCloneFlyout"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:FluentHub.Shared.Controls.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Loaded="OnGitCloneFlyoutLoaded"
	mc:Ignorable="d">

	<UserControl.Resources>
		<Style x:Key="CloneActionButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Padding" Value="12,8" />
			<Setter Property="MinHeight" Value="40" />
			<Setter Property="HorizontalAlignment" Value="Stretch" />
			<Setter Property="HorizontalContentAlignment" Value="Left" />
			<Setter Property="UseSystemFocusVisuals" Value="True" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Grid
							x:Name="RootGrid"
							Padding="{TemplateBinding Padding}"
							Background="{TemplateBinding Background}"
							CornerRadius="{ThemeResource ControlCornerRadius}">
							<Grid.BackgroundTransition>
								<BrushTransition Duration="0:0:0.083" />
							</Grid.BackgroundTransition>
							<ContentPresenter
								HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalContentAlignment="Center"
								Content="{TemplateBinding Content}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								Foreground="{TemplateBinding Foreground}" />
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal" />
									<VisualState x:Name="PointerOver">
										<VisualState.Setters>
											<Setter Target="RootGrid.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Pressed">
										<VisualState.Setters>
											<Setter Target="RootGrid.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Disabled">
										<VisualState.Setters>
											<Setter Target="RootGrid.Opacity" Value="0.4" />
										</VisualState.Setters>
									</VisualState>
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<StackPanel
		Width="420"
		Margin="0,8,0,0"
		Spacing="8">

		<StackPanel Margin="0,0,0,12" Spacing="16">
			<StackPanel Spacing="4">
				<TextBlock
					Margin="12,0"
					Style="{ThemeResource BodyStrongTextBlockStyle}"
					Text="HTTPS" />
				<Grid ColumnSpacing="8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>
					<TextBox x:Name="HttpsCloneUriTextBox" IsReadOnly="True" />
					<local:CopyButton
						x:Name="HttpsCopyButton"
						Grid.Column="1"
						Width="36"
						Height="32"
						CopiedMessage="HTTPS URL copied">
						<FontIcon FontSize="14" Glyph="&#xE8C8;" />
					</local:CopyButton>
				</Grid>
			</StackPanel>

			<StackPanel Spacing="4">
				<TextBlock
					Margin="12,0"
					Style="{ThemeResource BodyStrongTextBlockStyle}"
					Text="SSH" />
				<Grid ColumnSpacing="8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>
					<TextBox x:Name="SshCloneUriTextBox" IsReadOnly="True" />
					<local:CopyButton
						x:Name="SshCopyButton"
						Grid.Column="1"
						Width="36"
						Height="32"
						CopiedMessage="SSH URL copied">
						<FontIcon FontSize="14" Glyph="&#xE8C8;" />
					</local:CopyButton>
				</Grid>
			</StackPanel>

			<StackPanel Spacing="4">
				<TextBlock
					Margin="12,0"
					Style="{ThemeResource BodyStrongTextBlockStyle}"
					Text="GitHub CLI" />
				<Grid ColumnSpacing="8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>
					<TextBox x:Name="GitHubCliCommandTextBox" IsReadOnly="True" />
					<local:CopyButton
						x:Name="GitHubCliCopyButton"
						Grid.Column="1"
						Width="36"
						Height="32"
						CopiedMessage="GitHub CLI command copied">
						<FontIcon FontSize="14" Glyph="&#xE8C8;" />
					</local:CopyButton>
				</Grid>
			</StackPanel>
		</StackPanel>

		<MenuFlyoutSeparator Margin="-4,0" />

		<StackPanel Spacing="4">
			<Button
				Height="36"
				HorizontalAlignment="Stretch"
				HorizontalContentAlignment="Left"
				Background="Transparent"
				BorderThickness="0"
				Click="GitHubDeskButton_Click">
				<TextBlock VerticalAlignment="Center" Text="Open with GitHub Desktop" />
			</Button>

			<Button
				Height="36"
				HorizontalAlignment="Stretch"
				HorizontalContentAlignment="Left"
				Background="Transparent"
				BorderThickness="0"
				Click="OpenVSButton_Click">
				<TextBlock VerticalAlignment="Center" Text="Open with Visual Studio" />
			</Button>

			<Button
				Height="36"
				HorizontalAlignment="Stretch"
				HorizontalContentAlignment="Left"
				Background="Transparent"
				BorderThickness="0"
				Click="DownloadZipButton_Click">
				<TextBlock VerticalAlignment="Center" Text="Download ZIP" />
			</Button>
		</StackPanel>
	</StackPanel>
</UserControl>

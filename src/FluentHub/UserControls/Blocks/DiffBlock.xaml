<UserControl
    x:Class="FluentHub.UserControls.Blocks.DiffBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentHub.UserControls.Blocks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid
        Padding="12"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel Orientation="Horizontal" Spacing="8">

                <Button
                    Width="32"
                    Height="32"
                    Background="Transparent"
                    Style="{StaticResource ClearButtonStyle}">
                    <FontIcon FontSize="12" Glyph="&#xE70D;" />
                </Button>

                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{x:Bind ViewModel.ChangedFile.TotalLineCount, Mode=OneWay}" />

                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="{x:Bind ViewModel.ChangedFile.FileName, Mode=OneWay}" />

            </StackPanel>
        </Grid>

        <MenuFlyoutSeparator Grid.Row="1" Margin="-12,12" />

        <Grid Grid.Row="2">
            <TextBlock
                FontFamily="Cascadia Code"
                FontWeight="SemiLight"
                IsTextSelectionEnabled="True"
                Text="{x:Bind ViewModel.ChangedFile.Patch, Mode=OneWay}" />
        </Grid>
    </Grid>
</UserControl>
